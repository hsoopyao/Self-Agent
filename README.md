# 🤖 Self Agent — 智能文档问答助手

一个基于 **LangChain + ChromaDB + BGE Embedding + GLM-4-Flash** 构建的本地化智能助手，支持**内部知识库检索（RAG）**、**实时联网搜索**、**临时文件解读**和**ReAct 多步推理**，并提供友好的 Streamlit Web 界面。

---

## ✨ 核心功能

- 📚 **文档管理**  
  支持上传 `PDF` / `TXT` / `MD` 文件，自动切分、向量化并存入本地 ChromaDB，构建专属知识库。

- 🧠 **智能检索（RAG）**  
  基于 BGE 向量模型，对用户问题进行语义检索，精准匹配内部文档片段，并生成高质量回答。

- 🌐 **联网搜索**  
  集成 tavily 搜索引擎，可实时获取最新资讯、新闻、天气、股票等动态信息。

- 📎 **临时文件解读（会话级）**  
  在侧边栏上传临时文件（PDF/TXT/MD），当前会话中优先基于该文件内容回答，不持久化，保护隐私。

- 🔁 **ReAct 多步推理**  
  针对复杂问题（如“对比”、“分析”、“为什么”），自动进入“思考→行动→观察→思考”循环，调用多个工具综合回答。

- 💬 **多轮对话上下文管理**  
  支持多轮对话，自动进行 Token 计数和动态历史压缩，确保不超出模型上下文窗口。

- 🎨 **流式输出与可视化思考过程**  
  思考、工具调用、观察结果以不同样式分块显示，最终答案逐字输出，体验流畅。

- 🗂️ **多会话管理**  
  可同时维护多个独立会话，每个会话拥有独立的对话历史和临时文件（可选）。

- 📊 **Token 实时监控**  
  实时显示当前会话 Token 用量，接近上限时自动触发历史摘要压缩，并提示用户等待。

- ⚙️ **可视化设置**  
  提供独立的设置页面，用户可通过 UI 调整检索阈值、Token 上限、压缩比例、ReAct 触发关键词和模型名称，无需修改代码。

---

## 🏗️ 项目结构
```md
self-agent/
├── app.py                       # 主入口，包含导航和聊天界面
├── .env                         # 环境变量（API Key 等，需自行创建）
├── chroma_db/                   # 向量数据库持久化目录（自动生成）
├── data/                        # 启动时自动加载的 PDF（示例文档）
├── doc/                         # 文档
│   ├── startup.md               # Windows / macOS 启动指南
│   └── update.md                # 更新日志
├── init/                        # 初始化脚本
│   ├── build_vectorstore.py     # 构建向量数据库
│   ├── download_model.py        # 下载 BGE 检索增强模型
│   └── generate_sample.py       # 生成测试 PDF 文件
├── prompts/                     # 提示词模板
│   ├── general_system.txt
│   ├── general_user.txt
│   ├── rag_system.txt
│   ├── rag_user.txt
│   ├── react_system.txt
│   └── router_system.txt
├── src/
│   ├── pages/                   # 独立页面
│   │   ├── knowledges.py        # 文档管理页面
│   │   └── settings.py          # 设置页面（调优参数）
│   ├── vectorstore.py           # ChromaDB 向量库操作
│   ├── load_docs.py             # 文档加载与切分
│   ├── rag_chain.py             # RAG 生成链
│   ├── general_chat.py          # 联网搜索 + 通用对话
│   ├── direct_chat.py           # 纯闲聊
│   ├── router.py                # 意图路由
│   ├── react_agent.py           # ReAct 多步推理
│   └── context_manager.py       # Token 计数与历史压缩
├── requirements.txt             # Python 依赖
└── README.md                    # 项目说明
```

---

## 🚀 快速启动

项目推荐使用 **Python 3.12** 和 `.venv` 虚拟环境。Windows 与 macOS 的完整安装、配置及启动流程请查看：

👉 **[Windows / macOS 本地启动指南](./doc/startup.md)**

---
## 🧭 使用指南
### 主界面（聊天）
* 顶部导航栏可在 “聊天” 和 “知识库” 页面间切换。
* 侧边栏： 
  * 清空上下文窗口：重置当前会话历史。 
  * Token 实时显示：查看当前会话 Token 使用情况，超过阈值会触发上下文压缩机制。 
  * 当前配置：展示当前生效的所有调优参数。 
  * 会话临时文件：上传 PDF/TXT/MD（最多 3 个，仅当前会话有效），上传后提问优先基于该文件。
* 输入框：输入问题后回车，系统自动路由并生成回答。

### 知识库管理页面（文档管理）
* 上传文档：支持 PDF/TXT/MD，点击“导入文档”后自动切分向量化。
* 已导入文档列表：可查看文件名，点击 🗑️ 删除。

### 设置页面（配置调优）
所有可调参数集中在此页面，包括：
* 全局 RAG 检索阈值：控制内部知识库匹配的敏感度。
* 临时文件检索阈值：控制会话临时文件的匹配敏感度。
* 最大上下文 Token 数：超过此值将触发历史压缩。
* 压缩目标比例：压缩后 Token 降至 最大Token * 此比例。
* ReAct 触发关键词：当问题包含这些词时自动启用多步推理。
* 模型名称：切换智谱 BigModel 系列模型（需账户权限）。 

调整后点击“保存设置”立即生效，无需重启应用。

### 路由与推理模式
* 简单问题（问候、天气、常识）→ 直接路由至闲聊或搜索。
* 内部知识问题（公司政策、技术手册）→ RAG 检索回答。
* 复杂推理问题（包含“对比”、“分析”、“为什么”等）→ 自动进入 ReAct 多步推理：
  1. 💭 思考：分析问题，决定下一步行动
  2. 🔧 行动：调用工具（内部检索或联网搜索）
  3. 📊 观察：获取结果摘要，并标注可点击的出处链接
  4. 循环直到收集足够信息，生成最终答案

### 临时文件与会话隔离
* 每个会话上传的临时文件互不干扰（独立存储于 session_state）。
* 临时文件不会写入持久化向量库，刷新页面后需重新上传。
* 支持最多 3 个文件同时上传，自动合并检索。

## ⚙️ 配置调优
所有关键参数均可通过 设置页面 进行可视化调整，无需编辑代码文件。

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 全局 RAG 检索阈值 | 内部知识库匹配敏感度 | 0.5 |
| 临时文件检索阈值 | 会话临时文件匹配敏感度 | 0.3 |
| 最大上下文 Token 数 | 触发历史压缩的阈值 | 6000 |
| 压缩目标比例 | 压缩后 Token 降至 最大Token × 此比例 | 0.6 |
| ReAct 触发关键词 | 启用多步推理的关键词列表 | 对比,分析,为什么,总结,评价,比较,区别,影响,解析,解读,解释,研究 |
| 模型名称 | 智谱 BigModel 系列模型 | glm-4.7-flash |

## 📦 依赖清单
核心依赖见 requirements.txt，主要包含：
* langchain、langchain-core、langchain-community
* chromadb、sentence-transformers、transformers
* streamlit、tiktoken、pypdf、tavily-python
* python-dotenv、openai、httpx、aiohttp

## 🔧 更新内容

### v1.0 (2026-08-25 ~ 2026-08-31)
* 新增 ReAct 多步推理：支持复杂问题的自主思考与工具调用循环
* 新增可视化设置页面：所有调优参数可通过 UI 调整
* 新增 Token 实时监控：主界面顶部显示 Token 用量，自动触发历史压缩
* 新增多文件临时上传：支持同时上传最多 3 个文件
* 优化 ReAct 触发逻辑：仅对 rag 和 web 意图生效，避免闲聊误触
* 优化观察结果展示：自动生成摘要，出处以可点击链接形式展示
* 优化 UI 样式：思考、行动、观察过程以不同样式分块显示
* 优化上下文管理：动态 Token 计数 + 智能历史压缩
* 修复：内容安全过滤错误的友好提示
* 修复：ReAct JSON 解析容错增强

[更多更新日志追踪](./doc/update.md)

## 🤝 贡献与反馈
本项目为个人智能助手原型，欢迎提出改进建议或 PR。如有问题，请提交 Issue。

## 📄 许可证
MIT License
