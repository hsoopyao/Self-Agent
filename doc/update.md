发布阻断问题
1. 导入了不存在的 logger 模块，实测启动报 ModuleNotFoundError。
2. 保存用户消息的代码被注释，导致：
    - 用户问题不进入历史
    - 多轮上下文失效
    - Token 统计和历史压缩不准确
    - messages[:-1] 反而会丢掉最近的助手消息
3. 调用了 search_with_score，但没有导入；进入 RAG 路由会触发 NameError。
4. 等模块在导入阶段就创建 LLM。新用户未配置 BIGMODEL_API_KEY 时，应用可能在设置页面出现前就退出。
   语法编译和 pip check 虽然通过，但实际执行 import app 会立即失败，说明当前缺少最基本的启动冒烟测试。
   v1.1.1 建议同时修复
- 的两秒搜索超时实际上无效，线程上下文退出时仍会等待搜索完成。
- 异常后没有退出循环，同一个错误最多重复调用五次。
- 没有统一设置请求超时和重试策略。
- 普通聊天、联网聊天和 RAG 都保留模块级全局 LLM，设置页面切换模型后部分链路不会生效，还可能在不同 Streamlit 会话间共享首次配置。
- 内部知识库未命中后，会把原始问题发送给 Tavily。内部查询可能因此外泄，建议由用户明确选择是否转为联网搜索。
- 已拿到完整结果后逐字符 sleep 模拟流式，500 字会额外增加约十秒延迟。
-  当前导入路径错误，实测脚本无法运行； 还包含 Windows 路径且依赖未声明的 modelscope。

## v1.1.1 最近优化记录（2026-08-28）

### 优化一：聊天链路、联网搜索与 ReAct 稳定性

- [x] 将 Tavily 搜索从线程池伪超时改为客户端原生 HTTP 超时，默认 5 秒，可通过 `TAVILY_SEARCH_TIMEOUT` 调整。
- [x] ReAct 执行发生异常后立即结束，避免同一个错误重复调用五次并再次输出“超出最大思考步数”。
- [x] 在统一的 LLM 工厂中增加请求超时和重试策略：`LLM_REQUEST_TIMEOUT` 默认 60 秒，`LLM_MAX_RETRIES` 默认 2 次。
- [x] 普通聊天、联网聊天、RAG 和上下文摘要统一按实际请求创建 LLM，设置页切换模型后下一次调用立即生效。
- [x] 内部知识库未命中时不再自动将问题发送给 Tavily；只有用户明确写出“联网搜索”等授权词才允许外部搜索。
- [x] ReAct 增加提示层和工具执行层两层联网授权检查，未授权时阻止 `web_search` 执行。
- [x] 移除 ReAct 逐字符 `sleep` 的假流式输出，最终答案改为单块输出，减少额外等待和页面重绘。

### 优化二：修复聊天页面灰色重复文字

- [x] 修复每次提交新问题时，上一轮回答以灰色文字重复显示的问题。
- [x] 原因是上一轮动态流式输出占位符在下一次长时间 rerun 期间被 Streamlit 暂时保留。
- [x] 回答保存完成后立即执行一次短 rerun，将动态占位符转换为稳定历史消息；不会重复保存用户问题或助手回答。

### 已知问题与后续优化

- [ ] 智谱模型繁忙时可能返回 `429 / 1305`。后续可增加专用友好提示、指数退避和可选备用模型；当前统一重试策略不能保证在服务持续拥堵时成功。
